# Expo SDK 57 and paused Firebase currently expose optional peer ranges that
# do not overlap (React Native and AsyncStorage). Netlify must resolve the
# same tested tree used by staging CI without failing on those optional peers.
legacy-peer-deps=true
